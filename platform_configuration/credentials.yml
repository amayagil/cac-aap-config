---
controller_credentials:
  - name: "controller-{{ aap_organization }}"
    description: Automation Controller
    organization: "{{ aap_organization }}"
    credential_type: Red Hat Ansible Automation Platform
    inputs:
      username: admin
      password: "{{ controller_admin_password }}"
      host: "{{ caap_fqdn }}"

  - name: "pah-{{ aap_organization }}"
    description: Private Automation Hub
    organization: "{{ aap_organization }}"
    credential_type: Container Registry
    inputs:
      host: "{{ caap_fqdn }}"
      username: admin
      password: "{{ controller_admin_password }}"
      verify_ssl: false

  - name: "quay-{{ aap_organization }}"
    description: Quay registry
    organization: "{{ aap_organization }}"
    credential_type: Container Registry
    inputs:
      host: quay.io
      username: "{{ quay_registry_username | default(omit) }}"
      password: "{{ quay_registry_password | default(omit) }}"

  - name: "GitLab Pass-{{ aap_organization }}"
    organization: "{{ aap_organization }}"
    credential_type: "Source Control"
    inputs:
      username: "{{ gitlab_username }}"
      password: "{{ vault_gitlab_token }}"

  - name: "GitHub Token-{{ aap_organization }}"
    organization: "{{ aap_organization }}"
    credential_type: "Source Control"
    inputs:
      username: "{{ github_username }}"
      password: "{{ vault_github_token }}"

  - name: "NUC Machine Credential-{{ aap_organization }}"
    organization: "{{ aap_organization }}"
    credential_type: "Machine"
    inputs:
      username: "{{ machine_username }}"
      ssh_key_data: "{{ vault_ssh_key_private }}"
